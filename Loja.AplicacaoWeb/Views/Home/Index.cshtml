@model IEnumerable<Loja.Dominio.Entidades.Categoria>

@{
    ViewData["Title"] = "Lista de Categorias";
}

<div class=text-center>
    <h1>Lista de Categorias</h1>
<p>
    <a asp-controller="Home" asp-action="CriarCategoria" class="btn btn-primary">Nova Categoria</a>
</p>
<table id="myTable" class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Titulo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Titulo)
                </td>
                <td>
                    <a asp-controller="Home" asp-action="AlterarCategoria" asp-route-codigo="@item.Codigo" class="btn btn-success m-1">Alterar</a>
                    <a asp-controller="Home" asp-action="DetalharCategoria" asp-route-codigo="@item.Codigo" class="btn btn-primary m-1">Detalhes</a>
                    <a asp-controller="Home" asp-action="DeletarCategoria" asp-route-codigo="@item.Codigo" class="btn btn-danger m-1">Deletar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
</div>

@section Scripts{
<script>
     $(document).ready( function () {
     $('#myTable').DataTable();
    } );
</script>
}
